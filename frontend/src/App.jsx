// Scaffold placeholder. The real app (login, upload, ask, eval dashboard) is
// built on the frontend branch.
export default function App() {
  return <div style={{ fontFamily: "system-ui", padding: 24 }}>CiteGraph — loading…</div>;
}
